{
  "name": "fixgeni-backend",
  "version": "1.0.0",
  "private": true,
  "main": "dist/server.js",
  "scripts": {
    "build": "rimraf dist && tsc",
    "seed": "node dist/seed.js",
    "start": "node dist/server.js",
    "postbuild": "if [ \"$SEED_ON_DEPLOY\" = \"true\" ]; then echo \"Seeding...\" && node dist/seed.js; else echo \"Skipping seed\"; fi"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "rimraf": "^5.0.5"
  },
  "engines": {
    "node": ">=18.17"
  }
}
